# CS6013 — Lab 1: C Warmup Lab

**Due:** Fri Jan 9, 2026 11:59 PM  
**Language:** C (compile with `clang`, NOT `clang++`)

## Overview
This lab is a warmup exercise in C. The starter (skeleton) code contains function stubs that must be completed.
The lab also requires adding appropriate test functions to verify correctness.

## What I Did
- Completed all required function stubs in the provided skeleton code.
- Added test functions (as instructed in the skeleton) to validate behavior.
- Verified everything compiles and runs using `clang`.

## Files
- `*.c` — C source files (implementation + tests)
- `*.h` — Header files (function declarations)
- `README.md` — This file

> Note: Filenames may differ depending on the provided skeleton code.

## Build Instructions (clang)
Compile using `clang`:

```bash
clang -Wall -Wextra -Werror -std=c11 -g *.c -o lab1
